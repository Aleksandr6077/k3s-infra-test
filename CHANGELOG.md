## ✅ Выполненный технический долг (Этап 1: Наблюдаемость и Инфраструктура)

В ходе первого этапа рефакторинга стек мониторинга и логирования кластера `k3s` были устранены критические уязвимости безопасности (ИБ), настроена отказоустойчивость хранения данных и обеспечена прозрачность состояния хоста.

### 1. Мониторинг хоста и устранение конфликтов безопасности (`node-exporter`)
* ❌ **Проблема (Configuration & Security Bug):** Встроенный в чарт `kube-prometheus-stack` компонент `node-exporter` циклически блокировался на этапе `CreateContainer`. Причина — жестко зашитое двунаправленное монтирование корня хоста (`mountPropagation: Bidirectional`), которое блокировалось движком `containerd` в `k3s` из-за приватного режима маунтов операционной системы.
* 🔧 **Решение:** Встроенный DaemonSet был полностью отключен в `mon-values.yaml`. Т.е. мы разделили один большой монолитный Helm-чарт на независимые управляемые модули, вместо него развернут независимый, оптимизированный под `k3s` манифест (`node-exporter-k3s.yaml`). Режим проброса системных директорий (`/proc` и `/sys`) изменен на **`HostToContainer`**. Интеграция с оператором Prometheus настроена через прямой механизм `additionalScrapeConfigs` в обход несовместимых локальных OpenAPI-схем.
* 🟢 **Результат:** 
  * Риск компрометации хоста через `Bidirectional` маунт полностью устранен.
  * Агент запущен в ИБ-безопасном режиме **ReadOnly** (строго только для чтения).
  * В Grafana ожили и корректно отображаются живые графики железа сервера (`CPU Usage`, `Memory Usage`, `Disk Space`).

### 2. Защита конфиденциальных данных (Grafana Secrets)
* ❌ **Проблема (Hardcoded Credentials):** Пароль администратора Grafana (`adminPassword: "admin"`) хранился в Git-репозитории в открытом текстовом виде (PlainText), что является грубым нарушением стандартов безопасности и компрометирует контур мониторинга.
* 🔧 **Решение:** Конфигурация Grafana переведена на механизм использования встроенных секретов кластера через параметр `existingSecret`. Пароль полностью вычищен из файлов конфигурации.
* 🟢 **Результат:** Реальные доступы создаются администратором один раз вручную напрямую в защищенной базе данных Kubernetes (`k create secret generic grafana-admin-credentials`). В репозитории хранится чистый декларативный код, безопасный для публичного размещения.

### 3. Гарантия сохранности логов (Loki Persistence)
* ❌ **Проблема (Ephemeral Storage):** Сбор логов Loki работал в монолитном режиме `SingleBinary` во временной памяти контейнера. Любой плановый рестарт пода, обновление чарта или перезагрузка ноды приводили к безвозвратной потере всей истории логов приложений.
* 🔧 **Решение:** Конфигурация логов в `loki-values.yaml` полностью переработана. Настроено долгосрочное (stateful) хранение данных: активирован параметр `persistence.enabled=true` и подключен объект PVC на базе дефолтного класса `local-path`. Активирован встроенный компонент `gateway` (Nginx шлюз) для правильной внутренней маршрутизации поисковых запросов.
* 🟢 **Результат:** Проведен успешный краш-тест (симуляция аварии хоста): при принудительном удалении пода `loki-0` кубер автоматически перепримонтировал виртуальный диск к новому инстансу. Вся история логов успешно сохранилась, гарантирована сохранность исторических логов при любых аварийных сценариях.
### 📋 Статус подов инфраструктуры мониторинга после рефакторинга:

```text
NAME                                                     READY   STATUS    RESTARTS   AGE
alertmanager-kube-prom-kube-prometheus-alertmanager-0   2/2     Running   0          90m
kube-prom-grafana-878f999cb-21x6c                       3/3     Running   0          90m
kube-prom-kube-prometheus-operator-55ccf999cb-pkfzd     1/1     Running   3          2d
kube-prom-kube-state-metrics-64898bb6f4-qxgxx           1/1     Running   3          2d
loki-0                                                  2/2     Running   0          71m
loki-canary-pjxsq                                       1/1     Running   0          78m
loki-chunks-cache-0                                     2/2     Running   0          89m
loki-gateway-7dbfcfc68d-4mggx                           1/1     Running   0          78m
loki-results-cache-0                                    2/2     Running   0          89m
node-exporter-xjkfn                                     1/1     Running   0          34m
prometheus-kube-prom-kube-prometheus-prometheus-0       2/2     Running   6          19m
promtail-p9wx9                                          1/1     Running   3          71m
```
## ✅ 2. Changelog: Этап 2

## Fixed
- 🩺 Добавлены пробы Liveness Readiness в манифест app.yaml
- 🚀 Ansible/Variables: Заменена нестабильная переменная окружения `ansible_env.USER` на системные факты `ansible_user_id` и `ansible_user_dir`. Это исправило ошибку падения плейбука при создании директории `.kube` и копировании конфигурационных файлов, когда таски выполняются с привилегиями `become: true`.
- *Makefile/Sudo: Изменен флаг запуска плейбука с `-k` (пароль SSH) на заглавный `-K` (`--ask-become-pass`). Это устранило ошибку `sudo: a password is required` при локальном развертывании кластера на `localhost`.

### Changed
- **Makefile/Idempotency**: Таска `down` переписана на императивную проверку существования деинсталлятора (`if [ -f ... ]`). Теперь команда `make down` (и, как следствие, `make recreate`) работает идемпотентно и не падает с ошибкой `command not found`, если кластер k3s еще не был установлен в системе.

### 🛠 Выполнено: Перевод кластера в режим High Availability (HA)

### Было:
* **Архитектура All-in-One**: Кластер k3s разворачивался в минимальной конфигурации на одиночной ноде (`localhost`) прямо внутри WSL2.
* **Единая точка отказа (SPOF)**: Использовалась дефолтная встроенная СУБД SQLite. Падение или повреждение единственной управляющей ноды приводило к полной потере контроля над кластером и уничтожению состояния (State) инфраструктуры.
* **Ограничение локальной среды**: Классический сценарий HA требовал поднятия нескольких тяжелых виртуальных машин, что приводило к избыточному потреблению ресурсов CPU/RAM на рабочем месте разработчика.

### Стало:
1. **Распределенный кворум (Raft)**: Архитектура переведена на полноценный HA-кластер, состоящий из 3 независимых Control Plane нод. Синхронизация состояния и консенсус обеспечиваются встроенным в k3s механизмом распределенной базы данных (на базе Raft) со строгим нечетным кворумом.
2. **Контейнеризированный полигон (Docker-in-Docker)**: Вместо тяжелых виртуалок ОС Ubuntu с `systemd` развернуты официальные легковесные production-образы `rancher/k3s`. Это позволило запустить честную HA-топологию в контуре WSL2 с минимальным оверхедом по оперативной памяти.
3. **Единая точка входа (Балансировщик трафика)**: Перед управляющими нодами развернут контейнер с отказоустойчивым прокси-сервером **HAProxy**, выполняющий роль Network Load Balancer. Он слушает порт `6443` (Kubernetes API) на `localhost` хоста и распределяет запросы методом `roundrobin` между живыми мастерами.
4. **Сквозная автоматизация (Ansible + Docker API)**: 
   * Инфраструктурный слой декларативно описывается и поднимается через `docker-compose.yaml`.
   * Ansible-инвентарь переведен на нативный плагин `ansible_connection=docker`, что позволило управлять нодами напрямую через Docker API без необходимости разворачивать SSH-серверы внутри k3s.
   * Плейбук разделен на контексты: `localhost` автоматизирует рабочее место разработчика (создание директорий, алиасов, установка дебиан-пакета K9s), а таски для `k3s-master-1` автоматически извлекают (fetch), адаптируют и прописывают актуальный `kubeconfig` на хост для работы `kubectl` и `k9s` «в одну кнопку».

### Схема отказоустойчивости для стресс-тестов:
* Кластер устойчив к падению любой одной Control Plane ноды из трех.
* Балансировщик HAProxy опрашивает мастера каждые 2 секунды (`check inter 2s`). При симуляции аварии (например, `docker compose stop k3s-master-2`) трафик автоматически перенаправляется на оставшихся лидеров кворума без прерывания обслуживания API.

###  🪛 1.1. Интеллектуальный тайм-аут и авторизация эндпоинтов API K3s
* **Проблема:** В легковесных дистрибутивах Kubernetes (включая K3s) при определенных политиках безопасности или версиях эндпоинты `/readyz`, `/livez` и `/healthz` могут быть закрыты для анонимных запросов снаружи. Вместо кода `200 OK` они возвращают стандартный ответ безопасности `401 Unauthorized`. Старая проверка в Makefile ждала строго `200`, из-за чего процесс зависал намертво.
* **Решение:** Логика ожидания в `Makefile` была расширена. Теперь цикл `until` воспринимает как зеленый свет **оба кода ответа: и 200, и 401**. Раз API-сервер вернул `401 Unauthorized`, значит, он полностью проинициализировал свои внутренние цепочки аутентификации, проснулся и готов обрабатывать защищенные запросы. Дополнительно чекаем через `docker logs k3s-balancer --tail 20`.

Скрипт мгновенно завершает цикл и передает управление в Ansible, сводя время холодного старта к минимуму.

### 🪛 1.2. Оптимизация экспорта Kubeconfig в HA-матрёшке (WSL2 + Docker + K3s)
* **Проблема:** Стандартные модули Ansible (например, `ansible.builtin.slurp`) генерируют Python-скрипты и пытаются выполнить их внутри целевой системы. Однако официальный образ `rancher/k3s` является специализированным минималистичным окружением (микро-ОС на базе BusyBox). В нём физически отсутствуют интерпретатор Python и утилита `sudo` . Попытка принудительно исполнить сгенерированный Python-код через `/bin/sh` приводила к синтаксическим ошибкам оболочки (`AnsiballZ_slurp.py: syntax error`) и падению деплоя. Кроме того, в HA-кластере из 3-х мастеров роль выполнялась трижды, создавая Race Condition (состояние гонки) при перезаписи локального файла.
* **Решение:** Логика роли `k3s_kubeconfig` была полностью переписана с использованием паттерна **"Delegate to Localhost"** (Делегирование на локальный хост):
  1. **Прямое копирование через Docker CLI:** Вместо использования Python внутри контейнера, Ansible теперь выполняет команду `docker cp k3s-master-1:/etc/...` на самой машине WSL2. Метод абсолютно независим от ОС внутри контейнера и не требует наличия Python или прав `sudo`/`su` на мастере.
  2. **Устранение гонки данных:** Добавлен флаг `run_once: true`, который принудительно заставляет Ansible забирать конфигурационный файл строго один раз с первого доступного мастера, исключая троекратное дублирование таски.
  3. **Упрощение UX:** Файл конфигурации теперь сохраняется прямо в дефолтный путь `~/.kube/config`, что избавляет разработчика от необходимости вручную экспортировать переменные окружения. При этом `make down` точечно вырезает только этот локальный контекст, сохраняя остальные рабочие кубы в безопасности.

### 🪛 1.3 Рефакторинг слоя балансировки (Устранение SPOF)

### Рефакторинг слоя балансировки (Устранение SPOF)

### **Added:**

### Описание добавленных файлов инфраструктуры

В ходе рефакторинга в проект были добавлены следующие конфигурационные файлы:

* **`local-infra/haproxy/Dockerfile`** — Инструкция сборки отказоустойчивого образа балансировщика на базе `haproxy:2.8-alpine`. Настраивает права доступа, устанавливает пакеты для работы VRRP и зарезает права процессов до безопасных Linux Сapabilities (`NET_ADMIN`, `NET_RAW`) для исключения работы под `root`.
* **`local-infra/haproxy/entrypoint.sh`** — Скрипт инициализации контейнера. Динамически переопределяет приоритет ноды в конфигурации Keepalived на основе переменных окружения Docker Compose, запускает фоновый демон демона сети и передает управление основному процессу HAProxy.
* **`local-infra/haproxy/keepalived.conf`** — Декларативная конфигурация протокола VRRP. Задает параметры плавающего VIP (`172.20.0.10`), правила аутентификации между балансировщиками и логику работы скрипта-чекера здоровья HAProxy.


**Было:**  
Использовался один контейнер `k3s-balancer` намертво привязанный к IP `172.20.0.10`. При его падении весь кластер становился недоступен (`Single Point of Failure`).

**Стало (HA-Продакшен подход):**  
Развернуто два независимых балансировщика, работающих без root-прав через безопасную сборку (Alpine + Linux Capabilities `NET_ADMIN`, `NET_RAW`):
* `haproxy-1` (Основной, IP: `172.20.0.100`, приоритет Keepalived: 101)
* `haproxy-2` (Резервный, IP: `172.20.0.101`, приоритет Keepalived: 100)

Управление трафиком происходит через плавающий Виртуальный IP (**VIP: `172.20.0.10`**), который автоматически переключается между нодами по протоколу VRRP.

```text
           [ Внешний клиент / kubectl / k3s-агенты ]
                              |
         v------- [ Плавающий виртуальный IP (VIP) ] -------v

         |                    172.20.0.10                   |
         |                                                  |
  [ haproxy-1 (MASTER) ]                             [ haproxy-2 (BACKUP) ]

         |                                                  |
         o------------------------o-------------------------o
                                  |
                   [ k3s Master Ноды (Port: 6443) ]
```

### Контейнеризация и Безопасность (Dockerfile)

Для реализации этой схемы был собран кастомный безопасный образ. Внутри контейнера HAProxy процессы изолированы и работают от непривилегированного пользователя `USER haproxy`, а сетевые права ядра выданы точечно:

* **`Dockerfile`**: Базируется на `haproxy:2.8-alpine`. Через утилиту `setcap` бинарнику `keepalived` нарезаны минимальные Linux Capabilities (`cap_net_admin,cap_net_raw=+ep`), чтобы он мог вешать VIP на сетевой интерфейс и слать VRRP-пакеты без полных прав root на хосте. Заранее созданы PID-папки `/run/keepalived` с правами для пользователя `haproxy`.
* **`entrypoint.sh`**: Скрипт запуска, который динамически подставляет приоритет ноды из переменных окружения Docker Compose, запускает `keepalived` в фоне с кастомным PID-файлом и передает управление основному процессу `haproxy`.


### Изменения в IaC (Ansible)

* Из файла `ansible/site.yaml` вырезан лишний этап деплоя роли балансировщиков. Настройка сети перенесена в декларативный Docker Compose, что ускорило прогон плейбука.
* В инвентаре `ansible/hosts.ini` секция `[loadbalancers]` очищена от специфичных параметров подключения — балансировщики теперь рождаются полностью изолированными и самодостаточными.


### Сценарий стресс-тестирования (Failover)

Для проверки отказоустойчивости был сымитирован критический сбой основной ноды:

1. Запущен бесконечный опрос API Kubernetes через VIP-адрес:
   ```bash
   while true; do curl -k -s -o /dev/null -w "%{http_code}\n" https://172.20.0.10:6443; sleep 0.5; done
   ```
2. Произведено жесткое отключение контейнера `haproxy-1`:
   ```bash
   docker stop haproxy-1
   ```
3. **Результат теста**: В момент падения основной ноды проскочило всего 2 ошибки связи (`000`), после чего Keepalived на `haproxy-2` мгновенно перехватил VIP-адрес `172.20.0.10` менее чем за 1 секунду. Поток ответов `401` от API Kubernetes восстановился автоматически. Кластер выстоял.


## ✅ 3. Changelog: Этап 3

## 🔒 Secrets

### Added (Добавлено)
- **DevSecOps**: Внедрена гибридная Vault-стратегия(`Ansible-Vault`) для защиты инфраструктурных секретов (пароли Grafana, аутентификация Keepalived) с использованием алгоритма AES-256.
- **Automation**: В `Makefile` добавлены новые таргеты для управления секретами: `make vault-encrypt`, `make vault-edit`, `make vault-decrypt`, а также тесты синтаксиса и деплоя `make ansible-check`, `make ansible-test`, `make ansible-deploy`.
- **Infrastructure**: Создана структура папок `group_vars/all/` для разделения публичных конфигураций (`vars.yml`) и зашифрованных данных (`vault.yml`).
- **Security**: Настроен локальный автоматический подхват мастер-пароля через `.ansible_vault_pass.txt` (файл строго добавлен в `.gitignore`).

### Changed (Изменено)
- **Refactoring**: Из роли `k3s_kubeconfig` удален хардкод имени контейнера мастера (`k3s-master-1`). Имя контейнера теперь подставляется динамически через встроенную переменную `{{ inventory_hostname }}`.
- **CI/CD / Ansible**: Этап экспорта `Kubeconfig` переведен на работу с динамической группой хостов `k3s_masters` вместо одиночного хоста.
- **Kubernetes**: Логика деплоя теперь автоматически создает K8s-секреты с расшифрованными из Vault данными в пространстве имен `monitoring`.

## 🔨 Fixes

### Changed (Изменено)
- **Refactor**: Корневая директория проекта очищена от мусора. Все Kubernetes-манифесты и Helm values (`app.yaml`, `loki-values.yaml`, `mon-values.yaml`, `node-exporter-k3s.yaml`) перенесены в изолированную директорию `k8s/`.
- **Ansible**: Обновлены пути к манифестам в переменных, удалён избыточный таск ручного обновления кэша `helm repo update`.
- **Makefile**: Полностью переписана директива `.PHONY` (задекларированы все таргеты). Команда `make help` переведена на генерацию через `awk` с правильным форматированием и цветовой подсветкой команд.
Вот такой будет ответ от `make help`:
```text
Доступные команды в Makefile: 
  vault-encrypt     Зашифровать файл секретов Ansible Vault
  vault-decrypt     Расшифровать файл секретов (не рекомендуется коммитить)
  vault-edit        Редактировать секреты "на лету" (авто-шифрование при сохранении)
  ansible-check     Проверить синтаксис плейбука с учетом секретов
  ansible-test      Запуск плейбука в режиме тестирования (--check --diff)
  ansible-deploy    Накатить конфигурацию на хосты (Реальный деплой вручную)
  up                Поднятие всего стенда одной кнопкой (Инфраструктура + Ожидание API + Деплой)
  down              Уничтожение стенда с полной очисткой данных и контекстов K8s
  recreate          Быстрая и полная пересборка стенда с нуля (down + up)
  help              Показать этот список доступных команд
```

### Added
- **Dynamic Context Injection**: Добавлена жесткая привязка к `kubeconfig` и `context: default` во все таски роли `k8s_deploy`. Это исключило скрытый деплой Helm-чартов во внешние/дефолтные контексты хостовой машины (WSL2 / Docker Desktop).
- **Dynamic Project Paths**: Внедрена системная переменная `playbook_dir` в файле `roles/k8s_deploy/vars/main.yml`. Пути к манифестам теперь рассчитываются автоматически относительно корня репозитория, убирая жесткую привязку к локальному имени папки (например, `/pet2` в моем случае).
- **Keepalived Reload**: В роль `k8s_deploy` добавлены таски автоматического релоада демона ***Keepalived** внутри работающих контейнеров `haproxy-1` и `haproxy-2`. Отправка POSIX-сигнала `SIGHUP` (`killall -HUP keepalived`) позволяет балансировщикам на лету применять свежесгенерированные из Ansible Vault конфигурации без перезапуска самих контейнеров и прерывания сетевых соединений.

### Fixed
- **Idempotency in Kubeconfig**: Переписано регулярное выражение в таске замены IP-адреса сервера (`roles/k3s_kubeconfig/tasks/main.yaml`). Старый поиск `127.0.0.1` заменен на гибкий паттерн `server:\s+https://.*:6443`. Теперь таск успешно отрабатывает при повторных запусках, не ломая логику.
- **Node Exporter Manifest Structure**: Исправлена критическая ошибка валидации Kubernetes API (`Code 422 / Unprocessable Entity`) в манифесте `k8s/node-exporter-k3s.yaml`. Блоки хостовых томов (`proc` и `sys`) корректно вынесены из массива `containers` в изолированную секцию `volumes`, что восстановило связность с `volumeMounts`.

### Changed
- **Ansible Automation Reliability**: Благодаря стабилизации контекстов и исправлению структуры манифестов, плейбук успешно разворачивает полный стек мониторинга (Grafana, Prometheus Operator, Loki) и целевое приложение с первого раза за один проход плейбука.


## ✅ 4. Changelog: Этап 4

### Архитектурная миграция: переход из локального Docker-окружения в облако Yandex Cloud с использованием IaC

### Added
- Добавлен полноценный слой инфраструктуры как кода (IaC) в каталоге `terraform/` на базе `OpenTofu`.
- Описан манифест `providers.tf` с конфигурацией скачивания провайдеров через официальное российское зеркало (`terraform-mirror.yandexcloud.net`) в обход внешних блокировок.
- В файле `main.tf` декларативно описана сетевая топология: виртуальная сеть (`VPC`), изолированная подсеть `10.128.0.0/24` в зоне `ru-central1-a` и группы безопасности.
- Настроены каноничные `stateful` правила фильтрации трафика (`Security Groups`): открыт входящий SSH (22), HTTP/HTTPS (80/443), Kubernetes API (6443) снаружи и разрешен полный `ANY` трафик внутри подсети для межнодового общения.
- Добавлено динамическое развертывание вычислительных ресурсов (`yandex_compute_instance`) с использованием мета-аргумента `count` на базе экономичных инстансов с 20% гарантированной долей CPU (`core_fraction = 20`): 1 целевой хост для HAProxy и 3 независимых мастера для кластера `k3s`.
- Внедрен механизм автоматической генерации инвентаря Ansible (`local_file` + `templatefile` на основе шаблона `hosts.ini.tpl`), который на лету переписывает файл `ansible/hosts.ini` актуальными динамическими IP-адресами после каждого развертывания.

### Changed
- Локальное управляющее окружение внутри `WSL2` связано напрямую с облачным API через экспорт IAM-токенов, что позволило полностью отказаться от промежуточных тяжелых гипервизоров (VirtualBox/Vagrant).
- Успешно проведено сквозное тестирование связности: время параллельного развертывания 4 серверов с нуля составляет ~45 секунд, Ansible-модуль `ping` подтвердил стабильный коннект со всеми узлами.

### Removed
- Локальная привязка к Docker Bridge-сети `172.20.0.0/16` переведена в разряд легаси-конфигураций.

### ✅ Изменения в рамках Этапа 3: Переход на облачный балансировщик

#### Added
- Добавлен декларативный манифест сетевого балансировщика трафика `yandex_lb_network_load_balancer` на стороне `OpenTofu` для распределения входящих запросов к Kubernetes API (порт `6443`) между мастерами.
- Добавлен механизм динамического наполнения глобального блока переменных `[all:vars]` в шаблоне инвентаря `hosts.ini.tpl` значением сгенерированного IP-адреса балансировщика (`yandex_lb_ip`).
- Внедрен шаг предварительной валидации (`pre_tasks`) на хосте `localhost`, который на лету патчит локальный `~/.kube/config`, автоматически подставляя актуальный внешний IP облачного балансировщика.

#### Changed
- Локальная связка утилит трафик-менеджмента (`HAProxy и Keepalived`) полностью исключена из архитектуры облачного контура как избыточная и создающая единую точку отказа (`SPOF`). Ее функции делегированы нативному облачному балансировщику Яндекса.
- Конфигурация деплоя K3s переведена с жесткого хардкода имен хостов на динамические списки `groups['k3s_masters']`. Это позволило собирать etcd-кворум на базе любого количества нод независимо от их названия.
- Параметр `--tls-san` при инициализации кластера теперь динамически принимает IP облачного балансировщика, обеспечивая легитимность TLS-запросов со стороны локального разработчика.

#### Removed
- Полностью удален слой генерации конфигурационных файлов `haproxy.cfg` и конфигураций Keepalived для облачных хостов.

### ✅ Изменения в рамках Этапа 4: Комплексная безопасность Control Plane и рефакторинг Ansible-плейбуков

#### Added
- Добавлен входной массив переменных `admin_allowed_ips` в конфигурацию `OpenTofu` для безопасной параметризации списков доверенных IP-адресов управления.
- Внедрена локальная переменная `yc_internal_lb_healthchecks` со служебными CIDR-блоками Yandex Cloud (`198.18.235.0/24`, `198.18.248.0/24`) для обеспечения бесперебойной работы систем мониторинга сетевого балансировщика.
- Реализована динамическая сборка результирующего массива разрешенных адресов `k3s_api_allowed_cidrs` через функцию `concat()` для оптимизации структуры правил файрвола.

#### Changed
- **Полностью переработана логика ресурса группы безопасности `yandex_vpc_security_group.k3s_sg`**: открытый доступ ко всем управляющим портам `22` (SSH) и `6443` (Kubernetes API) сужен со вседоступного диапазона `0.0.0.0/0` до строго изолированных белых списков.
- **Оптимизирован процесс деплоя Helm-чартов мониторинга:** убран избыточный параметр `update_repo_cache: true` из тасок установки Loki и Prometheus, так как обновление кэша репозиториев теперь централизованно выполняется отдельным шагом `helm repo update`.
* **Infrastructure Optimization**: Все мастера K3s (`yandex_compute_instance.k3s_masters`) переведены на использование прерываемых виртуальных машин (`preemptible = true`). Это позволяет снизить затраты на инфраструктуру в Yandex Cloud (до 70%).


#### Fixed
- Исправлена критическая уязвимость (Security Misconfiguration) в правиле ingress для HTTPS, где отсутствие явного указания параметра `port` приводило к непреднамеренному открытию всего диапазона TCP-портов (`0-65535`) для входящего трафика со всего мира.
- Устранено состояние гонки (**Race Condition**) на этапе сборки высокодоступного (HA) кластера K3s. Внедрен модуль `ansible.builtin.wait_for` для ожидания старта API-сервера на первом мастере и добавлена директива `throttle: 1`. Это гарантирует последовательное подключение остальных нод строго по очереди и исключает split-brain в `базе данных`(Etcd).
- Ликвидирован мертвый код (**Legacy**) в роли `k8s_deploy`. Полностью удалены таски генерации локальных конфигураций для `HAProxy` и `Keepalived`, которые вызывали аварийную остановку рантайма из-за отсутствия целевой директории `../local-infra/haproxy`.
- Повышена идемпотентность и стабильность повторных запусков: параметр `creates` в тасках установки K3s перенаправлен с бинарного файла на финальный конфигурационный файл `/etc/rancher/k3s/k3s.yaml`.

#### 🐒 Инфраструктурное тестирование:
Для валидации внесенных изменений был проведен комплексный цикл сквозного тестирования:
- **Положительный тест (Positive Test):** Сетевой утилитой `nc` (netcat) верифицирована доступность портов `22` и `6443` с легитимного административного IP-адреса, переданного в рантайм через локальный файл `terraform.tfvars`. Трафик успешно авторизован файрволом (`Connection succeeded`).
- **Отрицательный тест (Negative Test):** Проведена имитация атаки извне путем подмены административного IP в правилах на сторонний CIDR (`1.1.1.1/32`). Повторный запуск сетевых проверок зафиксировал жесткий таймаут (`Connection timed out`), что подтверждает полную изоляцию Control Plane от несанкционированного доступа.
- **Валидация балансировщика:** Подтверждена корректность прохождения хелсчеков сетевого балансировщика Yandex Cloud со служебных подсетей после сужения прав. Ноды успешно перешли в статус `HEALTHY`.
- **Валидация идемпотентности и кворума:** Успешно протестирована последовательная инициализация нод Ansible-плейбуком.

https://gitverse.ru/swtr/rest/api/unit/files/v1/download?fileId=474feaa4-d807-45ca-9e6e-3c9cc76e4058

https://gitverse.ru/swtr/rest/api/unit/files/v1/download?fileId=c771f46d-3de0-4219-9605-c10df71ae830


### ✅ Изменения в рамках Этапа 4: S3 стейт + Изолированный гибридный контур + Фикс лимитов API Яндекса

#### 🟢 Добавлено (Added)
* **Отказ от CLI хардкода**: Логика авторизации переписана. OpenTofu теперь умеет на лету использовать IAM-токены Яндекса (`TF_VAR_yc_token`), генерируемые через `yc iam create-token` внутри `Makefile`, без жестко зашитых секретов в коде.
* **S3 Бэкенд для OpenTofu**: Настроена централизованная миграция локального стейта (`terraform.tfstate`) в удаленное S3-совместимое хранилище (Yandex Object Storage) для надежного хранения состояния.
* **Глобальный экспорт ключей в Makefile**: Реализовано автоматическое чтение статических ключей доступа из файла `sa_key.json` на глобальном уровне `Makefile`, что сделало все таргеты независимыми.
* **Топология гибридной сети (Публичная + Приватная подсети)**: Сетевая структура полностью разделена для борьбы с асимметричной маршрутизацией. Создана `k3s-public-subnet` (`10.200.1.0/24`) для бастиона и `k3s-private-subnet` (`10.200.2.0/24`) для изолированных мастеров.
* **Безопасный NAT-шлюз**: В приватный сегмент сети внедрен маршрутизатор `yandex_vpc_gateway` (NAT Gateway) и таблица маршрутизации `yandex_vpc_route_table`. Скрытые ноды получили безопасный выход в интернет для скачивания пакетов и Helm-чартов без выделения им публичных IP.
* **Паттерн Бастиона (Jump Host) для Ansible**: Первая нода `k3s-master-1` вынесена в публичную подсеть с динамическим `nat = true` для организации защищенной точки входа. Остальные мастера полностью изолированы.
* **Туннелирование SSH через ProxyCommand**: В шаблон `hosts.ini.tpl` интегрирована автоматическая генерация параметров `ProxyCommand`. Ansible теперь прозрачно настраивает приватных мастеров, пробрасывая SSH-сессии через белый IP первого мастера-бастиона.
* **Использование постоянного статического IP балансировщика**: Инфраструктура переведена на использование заранее зарезервированного статического IP-адреса через `data "yandex_vpc_address"` с извлечением объекта через функцию `one()`. Это полностью решило проблему с превышением квот Яндекса (`vpc.externalAddressesCreation.rate`) и намертво зафиксировало внешнюю точку входа в кластер для `kubectl`.

#### 🟡 Изменено (Changed)
* **Динамическое распределение хостов**: Логика создания `yandex_compute_instance.k3s_masters` адаптирована под тернарные операторы: первая нода автоматически улетает в публичную подсеть, ведомые мастера — в приватную.
* **Динамический таргетинг балансировщика**: Ресурс `yandex_lb_target_group` переписан на динамическое чтение `subnet_id` прямо из сетевых интерфейсов развернутых ВМ, что позволило объединить хосты из разных подсетей в один пул балансировки.
* **Оптимизация путей в Makefile**: Абсолютные хардкод-пути заменены на динамические относительные пути. Проект стал полностью переносимым и готовым к запускам в CI/CD-пайплайнах.
* **Разведение конфликтующих CIDR-диапазонов**: Внутренняя подсеть проекта переведена с дефолтного пула `10.128.0.0/24` на изолированные диапазоны `10.200.1.0/24` и `10.200.2.0/24`. Это устранило пересечение маршрутов с автоматической сетью `default` Яндекса.
* **Информативные аутпуты**: Из вывода OpenTofu полностью удален нерелевантный пустой массив публичных адресов, вместо него добавлен красивый вывод приватных IP-адресов мастеров `k3s_masters_internal_ips` и железный адрес балансировщика.

#### 🔴 Удалено (Removed)
* **Удален блок `depends_on` для IP**: Из манифеста виртуальных машин вырезана устаревшая зависимость ожидания создания IP-адреса, так как статический IP теперь считывается мгновенно через `data source` еще до старта сборки графа.

## ✅ 5. Changelog: Этап 5

### Добавлено
- **Разметка виртуальных машин** K3s Masters метаданными (`labels`) в Terraform для подготовки инфраструктуры к будущему внедрению честного `Dynamic Inventory`.
 - **Выделенный `Bastion-хост` для безопасного администрирования:** В инфраструктуру Terraform добавлен изолированный ресурс `yandex_compute_instance.bastion` минимальной конфигурации (`preemptible = true`), развернутый в публичной подсети. Он стал единственной легитимной точкой входа для SSH-сессий.
- **Строгая изоляция приватного контура SSH:** В файрвол yandex_vpc_security_group.k3s_sg добавлен ingress-обработчик, разрешающий входящий SSH-трафик (порт 22) на приватные мастера исключительно из CIDR-диапазона публичной подсети Яндекса, где локализован Bastion.

### Изменено / Исправлено
- **Исправление синтаксиса и типов данных в IaC:** Исправлен критический баг извлечения IP-адреса облачного балансировщика из вложенных множеств (Sets) провайдера Yandex Cloud. Синтаксис переписан с использованием встроенной функции `one()` (`one(one(yandex_lb_network_load_balancer.k3s_lb.listener).external_address_spec).address`). Это позволило передавать в шаблон инвентаря чистую строку IP вместо массива, что полностью устранило ошибку `undefined variable 'yandex_lb_ip'` в плейбуке Ansible.
- **Оптимизация пайплайна (Fail-Fast):** Намеренно прерван и зафиксирован в техническом долге деплой тяжелого стека мониторинга (Loki/Prometheus) через императивные роли Ansible. Логика деплоя приложений внутри K8s изолирована от слоя конфигурации ОС.
 - **Рефакторинг сетевой топологии Control Plane:** В манифесте `main.tf` устранен антипаттерн совмещения ролей шлюза и управляющей ноды. Ресурс `yandex_compute_instance.k3s_masters` полностью переведен в приватную подсеть `k3s_private_subnet`, а параметры публичного IP принудительно деактивированы (`nat = false`).
 - **Исправление синтаксиса ProxyCommand в IaC:** Исправлен критический баг интерполяции данных в шаблоне `hosts.ini.tpl.` Массив внутренних IP-адресов разделен по явным индексам ([0], [1], [2]). Это устранило склеивание строк, из-за которого клиент SSH внутри Ansible пытался некорректно инициализировать сессию на дефолтном порту 65535 и падал по таймауту. (проверка )

## ✅ 5. Changelog: Этап 6

### Добавлено

 - **Изолированный контур для Worker-нод:** В сетевую топологию `main.tf` добавлена новая приватная подсеть `k3s_workers_subnet (10.200.3.0/24)`, полностью отделенная от мастеров, с привязкой к существующему `NAT`-шлюзу Яндекса.
 - **Внутренний балансировщик (Internal NLB):** В инфраструктуру добавлен приватный сетевой балансировщик `k3s_internal_lb` со случайным приватным IP-адресом внутри подсети мастеров. Ресурс подготовлен как бесконфликтная заготовка (слушает порт 80) под будущую балансировку веб-трафика приложений на воркеры.
  - **Вывод приватного IP балансировщика:** В файл `outputs.tf` добавлен аутпут `k3s_internal_balancer_ip` для автоматической передачи приватного адреса NLB в конфигурационные файлы.
  - **Вывод публичного IP Бастиона:** В файл `outputs.tf` добавлен аутпут `bastion_public_ip`, что устранило необходимость вручную искать прыжковый адрес в сгенерированных инвентарях для SSH-подключения.

### Изменено

 - **Разделение Security Groups по ролям (Production-way):** Устранен антипаттерн «всё в одной куче», из-за которого наглухо зависал входящий SSH-трафик. Старая общая группа `k3s_sg` полностью удалена. Вместо неё созданы две изолированные группы безопасности: `bastion_sg` (только для прыжкового хоста) и `cluster_sg` (для мастеров и будущих воркеров), что исключило коллизии stateful-таблиц Яндекса.
  - **Динамический SSH-доступ по ID группы:** В правилах файрвола `cluster_sg` жесткий CIDR-диапазон подсети Бастиона заменен на динамическую привязку по ID группы безопасности (`security_group_id = yandex_vpc_security_group.bastion_sg.id`). Теперь доступ на приватные ноды разрешен строго участникам группы Бастиона, независимо от их подсетей.
   - **Исправление синтаксиса межнодового доверия в Yandex Cloud:** Исправлен критический баг схемы провайдера Яндекса, приводивший к ошибке unknown predefined target 'self'. Конфигурация переписана с использованием легитимного для Яндекс Облака значения параметра `predefined_target = "self_security_group"`, что обеспечило сквозное доверие между мастерами и воркерами.
   - **Безопасное устранение коллизии портов API:** Из описания внутреннего балансировщика полностью удален конфликтный блок `attached_target_group` мастеров на порт `6443`. Это решило проблему блокировки со стороны API Яндекса, запрещающего вешать одну таргет-группу на два NLB по одинаковым портам, и сохранило внешнее управление кластером.
   - **Рефакторинг генерации инвентаря и ликвидация хардкода индексов:** В `main.tf` полностью переписана логика передачи данных в ресурс `local_file.ansible_inventory` — вместо плоских списков IP теперь передаются объекты виртуальных машин целиком. Сам шаблон `hosts.ini.tpl` переведен на декларативные циклы `%{ for ... %}`. Это устранило жесткую привязку к фиксированному числу машин (индексы `[0]`, `[1]`, `[2]`), сделало инвентарь полностью «резиновым» и обеспечило автоматическое проксирование через Бастион для любого числа мастеров и воркеров без ручной правки кода.

## ✅ 5. Changelog: Этап 7

### Добавлено

- **Динамическое масштабирование Worker-нод через `for_each`:** В `variables.tf` объявлена переменная `k3s_workers` типа `map(object)`, описывающая структуру воркеров. В `main.tf` внедрен динамический запуск ВМ. Теперь добавление, удаление или изменение ресурсов нод происходит декларативно (в духе GitOps) без копирования блоков кода.
- **Автоматический сбор IP-адресов воркеров:** В `outputs.tf` добавлен аутпут `k3s_workers_internal_ips` со списком приватных IP всех активных воркеров. Массив генерируется через цикл `for` с учетом индекса интерфейса `[0]` для бесшовной передачи адресов в конфигурационные файлы (например, статический `hosts.ini` для Ansible).

### Изменено

- **Полная сетевая изоляция веб-контура воркеров:** Виртуальные машины воркеров перенесены из общей подсети мастеров в специализированную изолированную подсеть `yandex_vpc_subnet.k3s_workers_subnet` с диапазоном IP-адресов `10.200.3.0/24`.
- **Связывание воркеров с внутренним балансировщиком (Internal NLB):** Развернута динамическая целевая группа `yandex_lb_target_group.k3s_workers_group`, которая на лету собирает внутренние IP-адреса воркеров из их выделенной подсети. Сам балансировщик веб-трафика `k3s_internal_lb` и его приватный листенер также перенесены в подсеть воркеров (`10.200.3.x`) и настроены на HTTP-чек (`healthcheck`) по порту 80.
- **Исправление синтаксиса списков интерфейсов Яндекса:** Устранен критический баг `Unsupported attribute` в выводе переменных. Логика обращения к параметрам `network_interface` в `outputs.tf` переписана с обязательным указанием индекса первого элемента `[0]`.

## ✅ 5. Changelog: Этап 8

### Добавлено
* **Централизованная блокировка состояния (State Locking) через YDB**: В конфигурацию бэкенда успешно интегрирована база данных Yandex Database (Serverless) `ydb.tf` через DynamoDB-совместимый интерфейс (`dynamodb_endpoint`). Это полностью исключает риск параллельной перезаписи и повреждения удаленного `.tfstate` файла при одновременной работе нескольких инженеров или пайплайнов.
* **Автоматизированный таргет планирования (`make plan`)**: В глобальный `Makefile` внедрен новый алиас для сборки плана. Команда на лету запрашивает актуальный IAM-токен Яндекса (`yc iam create-token`), безопасно прокидывает его в переменные окружения OpenTofu и выводит кастомное цветное уведомление об успешном завершении операции.

### Изменено
* **Оптимизация идемпотентности манифестов (`ydb.tf`)**: Модуль базы данных очищен от избыточного кода создания таблиц. Документная таблица блокировок переведена под внешнее управление (Таблица создана через консоль), что полностью решило проблему рассинхронизации стейта и привело команду `make plan` к эталонному статусу `No changes`
*Пример проверки `make plan` одновремнно в 2 сессиях:*

 https://gitverse.ru/swtr/rest/api/unit/files/v1/download?fileId=30d5527d-b921-4023-8f44-2a30777b933c
 
Лок успешно работает:)


## ✅ 5. Changelog: Этап 9

### Добавлено
* **Изолированный бэкенд блокировок (State Locking):** В конфигурацию `backend "s3"` прописан выделенный эндпоинт распределенной базы данных YDB Serverless. Это гарантирует надежную защиту удаленного файла состояния `.tfstate` от гонки данных, одновременных запусков и повреждения структуры при конкурентной работе инженеров или пайплайнов.

### Изменено
* **Архитектурное разделение и защита базы блокировок:** Управление ресурсом базы данных полностью вынесено за рамки жизненного цикла основного стенда. Из папки `terraform` удален модуль `ydb.tf` и очищен стейт. Сама Serverless-база переведена на постоянное автономное хранение в облаке, что позволило беспрепятственно выполнять команду `make destroy` для K3s-кластера без риска случайного удаления инфраструктуры обеспечения блокировок и падения сессий с ошибкой `ResourceNotFoundException`.

## ✅ 5. Changelog: Этап 10

### Изменено
- **Декомпозиция монолита**: Файл `app.yaml` удален, его код разделен на атомарные манифесты в папке `k8s/apps/nginx/`:
  - `pvc.yaml` — вынесен слой хранения данных (PersistentVolumeClaim).
  - `deployment.yaml` — управление подами, лимитами ресурсов и политиками безопасности.
  - `service.yaml` — сетевой абстрактный слой (ClusterIP) для маршрутизации внутри кластера.
  - `ingress.yaml` — правила внешнего доступа к приложению через Ingress-контроллер.


## ‼️‼️Технический долг в рамках промежуточного этапа:

1. ~~‼️ Избавление от хардкода индексов виртуальных машин (`[0]`, `[1]`) в шаблоне `hosts.ini.tpl` и перевод генерации инвентаря на декларативные циклы `%{ for ... %}` для поддержки масштабируемости кластера.~~
2. ‼️ Полный переход с текстового файла `hosts.ini` на честный **Yandex Cloud Dynamic Inventory** через плагин `yc_compute`. (сейчас метод Terraform Push - он  сам создает виртуалки и перезаписывает файл hosts.ini)
3. ‼️ Развертывание **ArgoCD** внутри кластера K3s и миграция деплоя манифестов мониторинга (Loki, Grafana, Prometheus) в `GitOps`-пайплайны.
4. ‼️ ~~Добавим Worker-ноды, Развернем второй NLB для веб-трафика `(80/443)`~~ и Установим ArgoCD
5. ‼️ ~~Лок стейта S3 для исключения дрифта при одновремнном tofu apply~~
6. ‼️ ~~Yandex Database (Serverless) `ydb.tf` - добавить защиту от удаления~~
7. ‼️ Мультизональность!!!!!

## 🚦 Текущий статус проекта

Проект находится в процессе активной миграции с локального Docker-окружения в облачную отказоустойчивую инфраструктуру Yandex Cloud.

* **[x] Этап 1: Инфраструктурный слой (IaC)** — Полностью автоматизировано развертывание сети VPC, правил Security Groups и базовых виртуальных машин в Yandex Cloud через OpenTofu.
* **[x] Этап 2: Автоматический инвентарь** — Настроена автогенерация файла `hosts.ini` из шаблона. Ansible успешно проходит этапы авторизации, валидации связи (`ping`) и первичной настройки ОС.
* **[x] Этап 3: Интеграция облачного балансировщика** — Полный отказ от локальных компонентов балансировки (HAProxy/Keepalived). Внедрен нативный `yandex_lb_network_load_balancer` с выделением статического внешнего IP-адреса для **перманентного маппинга** входящего трафика Kubernetes API.
* **[ ] Этап 4: Развертывание мультизонного HA-кластера K3s (TBA)** — Переработка архитектуры вычислительных ресурсов: отказ от хардкода одной зоны в пользу распределения 3 мастер-нод по изолированным дата-центрам Yandex Cloud (`ru-central1-a`, `ru-central1-b`, `ru-central1-d`). Обеспечение физического кворума etcd (минимизация риска split-brain). Динамическая генерация сетевых подсетей и инвентаря Ansible через циклы по массиву зон.
* **[ ] Этап 5: Observability & Security (TBA)** — Развертывание нативного облачного стека мониторинга (Prometheus Operator / Grafana), централизованного логирования (Loki) и сквозной запуск проверок безопасности в CI/CD пайплайнах (Trivy, Линтеры).


